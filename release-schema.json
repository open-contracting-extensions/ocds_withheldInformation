{
  "properties": {
    "withheldInformation": {
      "title": "Withheld information",
      "description": "Details about withheld information.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/WithheldInformationItem"
      },
      "minItems": 1,
      "uniqueItems": true
    }
  },
  "definitions": {
    "WithheldInformationItem": {
      "title": "Withheld information item",
      "description": "Details of a withheld item of information.",
      "type": "object",
      "properties": {
        "id": {
          "title": "ID",
          "description": "The locally unique identifier for the withheld information item.",
          "type": "string",
          "minLength": 1
        },
        "field": {
          "title": "Field",
          "description": "The name of the withheld data field.",
          "type": [
            "string",
            "null"
          ],
          "minLength": 1
        },
        "name": {
          "title": "Name",
          "description": "The title of the withheld data field.",
          "type": [
            "string",
            "null"
          ],
          "minLength": 1
        },
        "rationale": {
          "title": "Rationale",
          "description": "The rationale for withholding this information.",
          "type": [
            "string",
            "null"
          ],
          "minLength": 1
        },
        "availabilityDate": {
          "title": "Availability date",
          "description": "The date the withheld information will be published.",
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "rationaleClassification": {
          "title": "Rationale classifications",
          "type": "array",
          "description": "Classifications of the rationale for withholding this information.",
          "items": {
            "$ref": "#/definitions/Classification"
          },
          "minItems": 1,
          "uniqueItems": true,
          "wholeListMerge": true
        }
      },
      "required": [
        "id"
      ],
      "minProperties": 1
    }
  }
}
